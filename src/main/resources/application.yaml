spring:
  application:
    name: airBnbClone

  datasource:
    url: jdbc:postgresql://localhost:5432/airBnb
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true